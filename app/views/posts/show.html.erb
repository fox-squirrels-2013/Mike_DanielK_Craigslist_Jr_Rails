<h4><%= link_to @category.name, "/categories/#{@category.id}", :class => "link" %></h4>
<h3><%= "#{@post.title} - #{@post.price}" %></h3>
<%= @post.description %><br><br>
<%= mail_to @post.email, "Contact vendor", :subject => @post.title, :class => "link" %>

